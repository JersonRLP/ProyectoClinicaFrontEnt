import { RemoveRolePrefixPipe } from './remove-role-prefix.pipe';

describe('RemoveRolePrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveRolePrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
